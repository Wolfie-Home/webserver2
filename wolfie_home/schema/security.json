{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "data about a security module",
    "type": "object",
    "required": ["Mag", "PIR", "Lgt"],
    "properties": {
	"Mag": { "type": "integer", "minimum": 0, "maximum": 1},
	"PIR": {"type": "integer", "minimum": 0, "maximum": 1},
	"Lgt": {"type": "integer"}
    }
}
