{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "data about a battery module",
    "type": "object",
    "required": ["Btyp", "Bat"],
    "properties": {
	"Btyp": {"type": "integer", "minimum": 0, "maximum": 1},
	"Bat": {"type": "integer", "minimum": 0,"maximum": 100}
    }
}

